# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '我的微博' do
  use_frameworks!

	
pod “AFNetworking”
pod “SDWebImage”
pod “YYModel”
pod “FMDB”
pod “SVProgressHUD”
pod “pop”

  target '我的微博Tests' do
    inherit! :search_paths
  end

  target '我的微博UITests' do
    inherit! :search_paths
  end

end
